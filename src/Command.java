/**
 * Interface that holds a standard way to execute the commands found in Command Controller
 * 
 * @author John Doggett
 *
 */
public interface Command {
	public abstract void exectuteCommand(CommandController control);
}
